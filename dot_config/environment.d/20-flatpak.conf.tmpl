# Flatpak exports application launchers and icons through these data paths.
# Keep the existing session paths, falling back to the XDG defaults.
XDG_DATA_DIRS={{ .chezmoi.homeDir }}/.local/share/flatpak/exports/share:/var/lib/flatpak/exports/share:${XDG_DATA_DIRS:-/usr/local/share:/usr/share}
